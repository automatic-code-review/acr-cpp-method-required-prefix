# acr-cpp-method-required-prefix

Arquivo config.json

```json
{
    "stage": "static",
    "rules": [
        {
            "prefix": "",
            "regexFile": "",
            "comment": "${METHOD_NAME} - ${FILE_PATH} - ${LINE}",
            "accessFilter": [],
            "methodIgnore": []
        }
    ]
}
```
